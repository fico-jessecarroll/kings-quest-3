{
  "name": "kq3-engine",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "predev": "npm run build:logic",
    "dev": "vite",
    "prebuild": "npm run build:logic",
    "build": "vite build",
    "build:logic": "tsx tools/compile-logic.ts",
    "discover-gaps": "tsx tools/discover-gaps.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
